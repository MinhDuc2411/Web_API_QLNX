@model IEnumerable<DATA_DuAn.DTO.DanhMucXeDto.DanhMucXeDTO>

<div class="container mt-5">
    <h2 class="text-center mb-4">List of All Xe</h2>

    <div class="d-flex justify-content-between align-items-center mb-4">
        <a class="btn btn-primary" asp-controller="DanhMucXe" asp-action="Create">
            <i class="bi bi-plus-lg"></i> Add New Xe
        </a>
        <form class="d-flex" asp-controller="DanhMucXe" asp-action="Index" method="GET">
            <div class="input-group">
                <select class="form-select" name="searchField">
                    <option value="BienSo">Biển Số</option>
                    <!-- Thêm các tùy chọn tìm kiếm khác nếu cần -->
                </select>
                <input type="text" class="form-control" id="id" name="id" placeholder="Nhập giá trị tìm kiếm" />
                <button type="submit" class="btn btn-secondary">
                    <i class="bi bi-search"></i> Tìm kiếm
                </button>
            </div>
        </form>
    </div>

    <div class="table-responsive">
        <table class="table table-bordered table-hover">
            <thead class="table-dark">
                <tr>
                    <th>MaXe</th>
                    <th>BienSo</th>
                    <th>HangXe</th>
                    <th>MauSac</th>
                    <th>GiaThue</th>
                    <th>TrangThai</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.MaXe</td>
                        <td>@item.BienSo</td>
                        <td>@item.HangXe</td>
                        <td>@item.MauSac</td>
                        <td>@item.GiaThue</td>
                        <td>@item.TrangThai</td>
                        <td>
                            <a class="btn btn-info btn-sm" asp-controller="DanhMucXe" asp-action="Details" asp-route-id="@item.MaXe">
                                <i class="bi bi-eye"></i> Detail
                            </a>
                            <a class="btn btn-warning btn-sm" asp-controller="DanhMucXe" asp-action="Edit" asp-route-id="@item.MaXe">
                                <i class="bi bi-pencil"></i> Edit
                            </a>
                            <a class="btn btn-danger btn-sm" asp-controller="DanhMucXe" asp-action="Delete" asp-route-id="@item.MaXe">
                                <i class="bi bi-trash"></i> Delete
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

@section Scripts {
    <script>
        // Add any JavaScript/jQuery if necessary for additional interactivity
    </script>
}
